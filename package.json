{
  "name": "pong",
  "version": "0.1.0",
  "description": "Simple browser Pong game",
  "main": "script.js",
  "scripts": {
    "dev": "live-server --port=8080 --open=./",
    "start": "live-server --port=8080 --open=./",
    "build": "tsc",
    "lint": "eslint . --ext .js,.ts --fix",
    "format": "prettier --write \"**/*.{js,ts,json,md,css,html}\"",
    "test": "jest --runInBand --coverage",
    "clean": "rimraf dist"
  },
  "keywords": [
    "pong",
    "game",
    "javascript"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^29.0.0",
    "eslint": "^8.0.0",
    "jest": "^29.0.0",
    "live-server": "^1.2.1",
    "prettier": "^2.0.0",
    "rimraf": "^3.0.0",
    "ts-jest": "^29.0.0",
    "typescript": "^4.0.0"
  }
}
